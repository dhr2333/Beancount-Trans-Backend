---
globs: project/apps/translate/views/*.py,project/apps/translate/services/handlers.py,project/apps/translate/services/parse/**/*.py,project/apps/translate/services/init/strategies/*.py,project/apps/translate/services/mapping_provider.py
alwaysApply: false
---
# translate：按账单类型划分模块边界

## 原则

- **与某一类账单强相关的业务逻辑**（字段含义、拼接匹配用文本、备注/金额/状态推导、本账单特有的账户或收支判定等）必须放在对应的 **`project/apps/translate/views/<账单>.py`** 中，以**无状态函数**或**小型数据结构**对外暴露，由调用方传入 `data`、映射列表、模型实例等依赖。
- **`project/apps/translate/services/handlers.py`**（如 `ExpenseHandler`、`AccountHandler`）**只做编排**：按 `bill_identifier` 分支，调用各账单模块的函数；避免在 handlers 内实现某一银行/平台的长逻辑或重复字段规则。

## 放置指引

| 内容 | 放置 |
|------|------|
| 某账单 CSV/PDF 列语义、对手方/附言/卡号所在列 | 对应账单的 `views/<Bill>.py` 或 `services/init/strategies/*_init_strategy.py`（与初始化记录结构一致） |
| 该账单专用的「映射关键字匹配全文」、收入对端资产解析等 | `views/<Bill>.py`（例如中行：`boc_debit_mapping_match_blob`、`boc_debit_try_income_peer_asset`） |
| 通用映射加载、相似度冲突解决、标签合并管线 | `handlers.py`、`mapping_provider.py` 等服务层 |

## 新增账单类型时

1. 在 `views/` 增加（或扩展）对应模块，集中该账单的解析与业务规则。
2. 在 `handlers.py` 等处仅增加**薄分支**与函数调用，不把新账单的大段逻辑写在 handlers 里。
3. 忽略规则、初始化策略仍按现有结构放在 `services/parse/ignore_rules/`、`services/init/strategies/`，与对应 `bill_identifier` 对齐。

## 参考

- 中行借记卡：`project/apps/translate/views/BOC_Debit.py`（账单专用匹配与收入对端资产判定）与 `handlers.py` 中的调用关系。
